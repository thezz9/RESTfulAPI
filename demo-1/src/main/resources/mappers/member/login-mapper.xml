<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.example.member.MemberDao">
 
 <select id="getLogin"   parameterType="com.example.member.MemberVO"
                         resultType="com.example.member.MemberVO" >
   
   select id as username, pwd as password , role, name ,grade, files, regdate
   from  member0718 where id = #{username}
   
 </select>	 

 
 </mapper>